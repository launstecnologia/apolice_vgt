RewriteEngine On

# Se o arquivo/pasta existir na raiz, serve direto
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Caso contrário, redireciona para /public
RewriteRule ^(.*)$ public/$1 [L]
